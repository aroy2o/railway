# Optimizer / AI microservice dependencies.
#
# Deliberately lean: libraries are added when the task that needs them lands,
# not up front.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0

# Google OR-Tools CP-SAT - the scheduling solver (PRD 10.3, task T6).
# Installed from task T1 onward so an install failure surfaces now rather than
# on the day the solver is being written.
ortools>=9.11,<10.0

# fulldata-ktv-psa branch only: the calibrated LightGBM risk model ported
# from full_data/release/integration_package/scoring.py replaces the linear-
# trend heuristic in app/core/risk.py. Pinned close to full_data's own
# requirements.txt so the pickled model/calibrator/transformer artifacts
# unpickle and score identically to how they were validated there.
pandas==3.0.1
# full_data pinned numpy==2.4.0, but that release was yanked upstream for a
# backward-compatibility bug - relaxed to a range in the same major version
# instead. numpy's own version does not affect the pickled model's behavior
# (unlike scikit-learn's, which is pinned exactly).
numpy>=2.1,<3.0
scikit-learn==1.9.1
lightgbm==4.7.0
joblib==1.5.3

# --- dev/test ---
pytest>=8.3,<9.0
httpx>=0.27,<1.0
anthropic>=0.40,<1.0
